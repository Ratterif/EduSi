import React from 'react';
import classes from './Neobutton.module.css';
import { Button } from '@mui/material';

export const NeobuttonBlue = (props) => (
  <button className={classes.NeobuttonBlue} {...props} />
);

export const NeobuttonGreen = (props) => (
  <button className={classes.NeobuttonGreen} {...props} />
);
